# rcon-client

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build rcon-client` to build the library.

## Running unit tests

Run `nx test rcon-client` to execute the unit tests via [Vitest](https://vitest.dev/).
